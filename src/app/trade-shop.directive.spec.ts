import { TradeShopDirective } from './trade-shop.directive';

describe('TradeShopDirective', () => {
  it('should create an instance', () => {
    const directive = new TradeShopDirective();
    expect(directive).toBeTruthy();
  });
});
